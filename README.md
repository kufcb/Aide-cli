# 这是什么
一个什么都可以做的智能体

# 配置
可在 `.env` 中配置以下开关：

- `TERMINAL_COMMAND_CONFIRM_ENABLED=true`
  - 默认开启。
  - 开启时，每次调用终端命令工具都会在执行前提醒并要求手动输入 `y/yes` 确认。
  - 设为 `false` 时，不再弹出确认，直接执行命令。

# 代做
1. 在一个会话中，把每次对话都的摘要记录在md文件中，找新对话和原对话的匹配程度(向量？) 如果匹配则加载到上下文，除非用户指定不需要。
2. 完成 skills 的外部扩展接口
3. 完成 tools 的外部扩展接口
